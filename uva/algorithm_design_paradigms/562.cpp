#include <bits/stdc++.h>

using namespace std;

int main(void) {
	int tc;
	scanf("%d", &tc);
	while(tc--) {
		int n;
		for(int i = 0; i < n; ++i) {
			scanf("%d", &coins[i]);
		}

		int knapsack[n + 1][n + 1][2];
		for(int i = 0; i < n; ++i) {
			for(int j = 0; j < n; ++j) {
				for(int k = 0; k < 2; ++k) {
					
				}
			}
		}
	}

	return 0;
}
